export const metadata = {
  title: "System Structure",
  description: "MinKowskiM system structure. Click a step to see related posts (OR across multiple categories/tags) on the same page.",
};

export default function StructureLayout({ children }) {
  return children;
}
